from . import stmts as stmts, types as types, typeinfer as typeinfer
from ._dialect import dialect as dialect
from ._interface import (
    repeat as repeat,
    set_detector as set_detector,
    set_observable as set_observable,
)
